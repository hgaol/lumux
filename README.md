# lumux — GitHub Pages

This is the `gh-pages` branch: the static landing page for lumux, served at the project Pages URL.

It is an **orphan branch** — it intentionally shares no history with `main`. The site is a single self-contained `index.html` (no build step, no dependencies).

Do not merge this branch into `main`.
