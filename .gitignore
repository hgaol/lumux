/target
**/*.rs.bk
Cargo.lock
/dist
.intentionally-empty-file.o

# local-only release helper (bumps sibling tap/bucket repos)
/bump-packages.sh

# developer-local Make targets (see `-include local.mk` in Makefile)
/local.mk
